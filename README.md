
## ✅ Atualizações Recentes

### 🔐 Sistema de Login
- Implementado um sistema completo de login com validação de credenciais.
- Integração com `models/user_account` para autenticação via dicionário de usuários.

### ✅ Correção de Dados
- Corrigido o uso da chave `'name'` para `'username'` no `userDic` localizado em `models/user_account.py`.

### 💡 Verificação no Front-End
- Criado um novo script JavaScript para validar os campos do formulário de login antes do envio.

### 🧭 Interface de Navegação
- Implementada a view `views/menu`, responsável por apresentar o menu principal após login bem-sucedido.
